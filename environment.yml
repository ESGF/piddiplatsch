name: piddi
channels:
  - conda-forge
dependencies:
  - pip
  - python>=3.11
  - flask
  - python-confluent-kafka>=2.9
  - pydantic>=2.11
  - multiformats
  - rich
  - requests
  - jsonpatch
  - toml
  - tqdm
  - humanize
  # dev
  - pytest
  - pip:
      - millify
