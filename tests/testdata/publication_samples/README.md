# Publication samples

These compact fixtures were extracted from
`outputs/dump/dump_messages_2026-08-25.jsonl`. They retain the real publication
envelope, dataset identity, source PIDs, and one complete data asset per project.
Large descriptive property sets and additional assets were omitted so the
fixtures remain reviewable. The production dump itself is intentionally not
committed.
